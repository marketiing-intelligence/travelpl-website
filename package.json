{
  "name": "travelpl-website",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local node_modules/astro/astro.js dev",
    "seed": "node --env-file-if-exists=.env.local --import tsx scripts/seed-blob.ts",
    "build:pwa": "cd ../wieruszow-app && npm run build",
    "copy:pwa": "rm -rf public/app && cp -r ../wieruszow-app/dist public/app",
    "build:full": "npm run build:pwa && npm run copy:pwa && astro build",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.1",
    "@astrojs/tailwind": "^6.0.2",
    "@astrojs/vercel": "^9.0.5",
    "@vercel/blob": "^2.4.0",
    "astro": "^5.17.1",
    "resend": "^6.9.3",
    "tailwindcss": "^3.4.19"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
